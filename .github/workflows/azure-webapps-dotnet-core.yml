# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy ASP.Net Core app to Azure Web App - OnlyBalds

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-onlybalds:
    runs-on: ubuntu-latest
    needs: deploy-api

    steps:
      - uses: actions/checkout@v4

      - name: Set up .NET Core
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.x'

      - name: Generate build-info.html
        run: |
          pwd
          ls
          COMMIT_HASH=$(git rev-parse --short HEAD)
          BUILD_DATE=$(date +"%Y-%m-%d %H:%M:%S")
          echo "<html>" > ./OnlyBalds/wwwroot/build-info.html
          echo "<head><title>Build Info</title></head>" >> ./OnlyBalds/wwwroot/build-info.html
          echo "<body>" >> ./OnlyBalds/wwwroot/build-info.html
          echo "<h1>Build Info</h1>" >> ./OnlyBalds/wwwroot/build-info.html
          echo "<p><strong>Version:</strong> $COMMIT_HASH</p>" >> ./OnlyBalds/wwwroot/build-info.html
          echo "<p><strong>Date:</strong> $BUILD_DATE</p>" >> ./OnlyBalds/wwwroot/build-info.html
          echo "</body></html>" >> ./OnlyBalds/wwwroot/build-info.html

      - name: Build with dotnet
        run: dotnet build -c Release

      - name: Build with dotnet
        run: dotnet build -c Release

      - name: dotnet publish
        run: dotnet publish -c Release -o ${{env.DOTNET_ROOT}}/myapp

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: .net-app
          path: ${{env.DOTNET_ROOT}}/myapp

  deploy-onlybalds:
    runs-on: ubuntu-latest
    needs: build-onlybalds
    environment:
      name: 'Production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}
    permissions:
      id-token: write #This is required for requesting the JWT

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: .net-app
      
      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_02CE699C522A4743A4CCE9F4CA01DB49 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_6E95F735D1BF46E6B33F9CEDFEB94768 }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_E89DCEE49AAF45FDBDE4EAFD65ED6148 }}

      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'OnlyBalds'
          slot-name: 'Production'
          package: .

  build-api:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up .NET Core
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.x'

      - name: Build with dotnet
        run: dotnet build --configuration Release

      - name: dotnet publish
        run: dotnet publish -c Release -o ${{env.DOTNET_ROOT}}/myapi

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: .net-api
          path: ${{env.DOTNET_ROOT}}/myapi

  deploy-api:
    runs-on: ubuntu-latest
    needs: build-api
    environment:
      name: 'Production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}
    permissions:
      id-token: write #This is required for requesting the JWT

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: .net-api
      
      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_0AD3B34606BB423880D62D055EB9FFF7 }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_B04AFB8FB69C4B78908DDBB2B7DA56BC }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_C7E00FA36F4C400D9290A92543D8B4DB }}

      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'OnlyBaldsApi'
          slot-name: 'Production'
          package: .